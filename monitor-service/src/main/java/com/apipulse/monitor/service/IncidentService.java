package com.apipulse.monitor.service;

import org.springframework.stereotype.Service;
import com.apipulse.monitor.entity.EndpointEntity;

@Service
public class IncidentService {
    public void openIncident(EndpointEntity endpoint) { //todo разобраться как их создавать и тд
    }

    public void closeIncident(EndpointEntity endpoint) {
    }
}
